{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy-hosting": "npx cross-env NITRO_PRESET=firebase yarn build && firebase deploy"

  },
  "devDependencies": {
    "firebase-admin": "^10.2.0",
    "firebase-functions": "^3.21.1",
    "firebase-functions-test": "^2.0.2",
    "nuxt": "3.0.0-rc.3"
  },
  "engines": {
    "node": "14.x || >=16.x"
  }
}
